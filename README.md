# pred-matcher

Prediction-market relation engine for finding equivalent, threshold-nested, and time-nested contracts across venues.

> Project status: bootstrap. The first working service implementation is developed on feature branches.

## Versioning

This project follows [Semantic Versioning](https://semver.org/). The first MVP release is `0.1.0`.
